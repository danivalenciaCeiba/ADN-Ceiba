import { EstadoCargaPipe } from './estado-carga.pipe';

describe('EstadoCargaPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoCargaPipe();
    expect(pipe).toBeTruthy();
  });
});
